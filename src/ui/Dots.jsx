import styled from "styled-components";

const Dots = styled.div`
  position: absolute;
  bottom: 3rem;
  left: 20%;
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 3rem;
  z-index: 5;
`;
export default Dots;
