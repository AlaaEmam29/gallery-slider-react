import React from "react";
import styled from "styled-components";
const SvgTheme = styled.img`
  display: block;
  width: 3rem;
  height: 3rem;
  object-fit: cover;
  cursor: pointer;
  fill: var(--color-dark);
`;

export default SvgTheme;
